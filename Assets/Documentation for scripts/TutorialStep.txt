================================================================================
  TutorialStep.cs
  Path : Assets/Scripts/Tutorial/TutorialStep.cs
  Type : [Serializable] classes (NOT MonoBehaviours), stored by [SerializeReference]
  Layer: Tutorial
================================================================================

PURPOSE
-------
One beat of a tutorial. A step is a coroutine: it sets the overlay up, waits for
its condition, then tidies up. TutorialRunner just walks the list, so adding a
new KIND of beat means adding a class here and nothing else.

    abstract class TutorialStep
        string note                       inspector-only label
        IEnumerator Run(TutorialRunner)   the beat itself

THE STEPS
---------
  TutorialCaptionStep
      text, hideCaptionWhenDone, until
      Shows a line of text and waits. No hand.

  TutorialHandDragStep
      caption, from (TutorialTarget), to (TutorialTarget), timings, until,
      hideCaptionWhenDone
      Loops a press-drag-release between two FIXED targets. Endpoints are
      re-resolved every cycle, so a target that moves stays pointed at.

  TutorialHandTapStep
      caption, at (TutorialTarget), timings, until, hideCaptionWhenDone
      Loops a tap in place. For "press this button" tutorials.

  TutorialMatchGuideStep          <- what the first tutorial actually uses
      caption, timings, until (default BoardEmpty), hideCaptionWhenDone,
      firstPairAtCell (default (-1,-1) = automatic)
      Points at whatever match the BOARD currently offers instead of at
      hard-coded cells. Every hand cycle re-asks, so:
        - drag the hinted stack somewhere else and the hint re-aims from its NEW
          position;
        - blast a pair and the hint moves on to the NEXT pair, all the way to an
          empty board, with no step authored per pair.
      It holds a "sticky" mover across cycles so the hand keeps dragging the SAME
      stack rather than flipping to whichever direction is shortest this frame.
      firstPairAtCell names the stack to teach first, because the shortest drag
      is not always the clearest first lesson.

  TutorialFocusTapStep            <- what the guided ONBOARDING uses
      tooltip, tooltipSide, at (TutorialTarget), holePadding, dimAlpha, timings,
      until (default AnchorClicked), resolveTimeout, giveUpAfterSeconds,
      keepGateAfter, settleAfter
      A tooltip and a pointing hand on ONE element, with every other pixel of the
      screen dead. Three things make it survive the real menu:

        1) IT WAITS for the target to be present AND able to take a click.
           MainMenuPanelController turns blocksRaycasts off on the whole panel
           for the 0.35s of its slide, so a hand shown on arrival would point at
           a dead button for a third of a second. Fighting those CanvasGroups is
           useless - the controller rewrites them on every tab change - so the
           step simply holds.

        2) Target, hole and tooltip are resolved through a LIVE lookup every
           frame, never cached: the first deployed card is Instantiated fresh on
           every panel open, and the Upgrade button is two different GameObjects
           that swap by affordability.

        3) The gate stays shut BETWEEN beats (keepGateAfter). Releasing it per
           step would hand the player a fully live screen during the panel tween
           the next beat is still waiting on. Turn it OFF on the last step.

      An AnchorClicked `until` with no anchorId of its own defaults to the
      anchor the step is pointing at, which is what it always means.

  TutorialWaitStep
      seconds. Dead time, e.g. to let a blast animation finish.

HOW IT CONNECTS
---------------
  - Lives inside TutorialSequenceSO.steps.
  - Calls back into TutorialRunner for target resolving, the hand, the caption,
    the tooltip, the focus gate and condition waiting. See TutorialRunner,
    TutorialFocusGate, TutorialTooltip and TutorialBoardHints.

NOTES
-----
  - These are plain classes, not ScriptableObjects, on purpose: one asset per
    tutorial instead of one asset per step.
  - Because [SerializeReference] matches by type name, renaming a step class
    silently empties that row in every existing sequence asset.
  - TutorialFocusTapStep NEVER treats a timeout as success. Both resolveTimeout
    (target never appeared) and giveUpAfterSeconds (player never acted) release
    the screen and call runner.AbortSequence(), which deliberately does not mark
    the tutorial as seen - so it gets another chance later.
