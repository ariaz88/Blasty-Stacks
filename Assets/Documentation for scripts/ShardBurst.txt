================================================================================
  ShardBurst.cs
  Path : Assets/Scripts/Puzzle/Match/ShardBurst.cs
  Type : MonoBehaviour  (self-bootstrapping scene-level VFX manager, singleton)
  Layer: Puzzle / Match
  Scenes: none - it creates itself at runtime
================================================================================

PURPOSE
-------
The match-clear shatter effect. Replaces the per-particle GameObject spraying in
FractureObject with a small pool of mesh-particle systems.

REBUILT 2026-08-26 against the Blasty Stacks reference (five frames supplied by
the user). The arc it now reproduces:

  1. MatchResolver collapses the block to a small but STILL-VISIBLE nub per
     board cell, then destroys it. Nothing here has fired yet. (reference fr.1)
  2. Each cell spawns a DENSE CLUMP of shards - overlapping but individually
     readable. A two-cell piece gives TWO clumps, never one merged cloud. (fr.2)
  3. The clumps rise and spread, bounded sideways and upward. (fr.3, fr.4)
  4. Past the apex they fall below the board, shrinking and fading. (fr.5)

Agreed envelope: +/-1 cell sideways, 1.2 cells up, ~2.25 cells down, 0.58 s.

!! THE ARC IS AUTHORED IN CELLS AND SECONDS, AND THE PHYSICS IS DERIVED FROM IT.
That is the whole design of this file - see "THE ARC MATHS" below. The previous
version authored raw speeds and a gravity modifier, which meant the bounds the
reference calls for could only ever be approximated by eye.

NOTHING TO WIRE
---------------
A [RuntimeInitializeOnLoadMethod] (AfterSceneLoad) creates a "~ShardBurst"
GameObject if none exists, marked DontDestroyOnLoad. Add the component to a
scene by hand ONLY if you want per-level tuning of the serialized fields.

  - Shard meshes  : Resources.LoadAll<Mesh>("VFX")  -> Assets/Resources/VFX/
                    These are 3D SOLIDS: 36-60 verts, 12-20 tris, 6-7 distinct
                    face normals each, real depth on Z. Spawned with random
                    rotation on all three axes.
  - Materials     : ONE PER POOLED SYSTEM, built at runtime from
                    Shader.Find("Blasty/ShardUnlit"). Not one shared material -
                    see the note below, this matters.
  - Colour palette: copied off any FractureObject already in the scene. This is
                    a LAST-RESORT fallback only; colour normally arrives per
                    burst from PieceTintSampler. See NOTES.

PUBLIC API
----------
  Play(Vector3 centre, Vector2 footprintCells, int colorId,
       PieceTintSampler.TintBands? bands)
      The real entry point. footprintCells is the cleared piece's bounding box
      in board cells, e.g. (2,1) for a 2-wide piece. Scales both the emitter box
      and the shard count. `bands` carries the block's own dark/body/light
      colours; pass null to fall back to the palette lookup by colorId.

  Explode(Transform origin, int colorId)
  ExplodeAtPosition(Vector3 worldPosition, int colorId)
      Signature-compatible with FractureObject so old call sites keep working.
      Both wrap the position into a single-cell list.

  Initialize()
      Loads meshes, builds the materials, fills the pool. Called from Awake;
      public and idempotent so a loading screen can pre-warm it.

THE ARC MATHS  (the important part of this file)
------------------------------------------------
Everything is authored in BOARD CELLS and SECONDS, and Play() solves for the
Shuriken values. Pure ballistics, no drag on any axis, so each bound is exact
rather than tuned.

A shard launched at vy under gravity g rises h = vy^2/2g in vy/g seconds, then
falls to depth d in sqrt(2(h+d)/g). Requiring rise + fall == totalDuration:

    T = sqrt(2h/g) + sqrt(2(h+d)/g) = sqrt(2/g) * (sqrt(h) + sqrt(h+d))
 => g  = 2 * (sqrt(h) + sqrt(h+d))^2 / T^2
    vy = sqrt(2*g*h)

  !! apexHeightCells, fallDepthCells and totalDuration TOGETHER PIN GRAVITY.
  !! There is no fourth knob and adding one lets the three disagree. An earlier
  !! version authored riseTime as well; the resulting gravity (65 u/s^2) threw
  !! the shards 16 cells below the board instead of 2.25.

HORIZONTAL is an IMPULSE that the X/Z drag then spends, NOT a constant drift:
    vxImpulse = spreadOutCells * cell * SpreadImpulseGain
  !! SpreadImpulseGain (7.5) is CALIBRATED BY MEASUREMENT against the limitX /
  !! dampen values in BuildSystem. Change those and it must be re-measured. It
  !! is linear in the impulse, so one simulate-and-measure pass fixes it.

  Why an impulse and not a drift: with a constant vx, displacement is linear in
  time, so a shard has covered only ~37% of its spread by the apex and the cloud
  is still a tight blob exactly when the reference is at its widest. The impulse
  puts the cloud at full width by t~0.16 and it then coasts.

  Per shard the sideways magnitude is remapped to [0.40, 1.0] of that impulse:
  a plain Random.Range(-1,1) leaves a fat band of shards near zero that never
  leave the middle, which was most of why the cloud read as clumped.

  Launch speed spread is WIDE (0.55 - 1.0 of vy) so the cloud breaks up instead
  of rising as one body. Low-vy shards peak early and low; the per-shard lifetime
  below still lands every one of them on the floor of the box.

PER-SHARD LIFETIME is solved from that shard's OWN launch speed:
    life_i = (vy_i + sqrt(vy_i^2 + 2*g*d)) / g
  !! Do not replace this with a flat lifetime. Launch speed is randomised for
  !! variety, and a slower shard peaks lower, therefore falls for LONGER, and
  !! lands well below the rest - measured at -3.5 cells against a -2.25 target.
  !! Solving per shard makes every one die exactly on the floor of the box.

INSPECTOR FIELDS
----------------
  Look    (retuned 2026-08-26: 15% fewer shards, each 20% bigger)
    shardsPerCell      (int, default 34)   shards per board cell of the piece
    maxShardsPerBurst  (int, default 136)  ceiling regardless of group size
    shardSizeRange     (Vector2, 0.12-0.34) world units; board cell is ~1.086

  Arc (board cells / seconds)
    clusterRadiusCells (float, 0.18)  radius of the dense per-cell clump (fr.2)
    spreadOutCells     (float, 1.00)  sideways bound (fr.3/4)
    apexHeightCells    (float, 1.20)  upward bound (fr.3/4)
    fallDepthCells     (float, 2.25)  how far below the cell it dies (fr.5)
    totalDuration      (float, 0.58)  shards appearing -> last one gone
                                      (was 0.85; shortened 2026-08-26)

  Sorting / Pool / Colours as before (sortingOrder 20, poolSize 6, palette).

PARTICLE SETUP  (all built in code in BuildSystem)
--------------------------------------------------
  Emission       NONE from the module - no bursts, shape DISABLED. Play() emits
                 every shard explicitly with ps.Emit(EmitParams, 1). That is the
                 only way to get one tight clump PER BOARD CELL and a per-shard
                 launch vector and lifetime out of a single system.
  Limit Velocity separateAxes ON. limitX / limitZ 0.25, dampen 0.28, and
                 limitY 10000 (i.e. effectively OFF on Y).
                 !! Y MUST stay unlimited. Play() derives gravity and launch
                 !! speed analytically for the vertical arc and damping Y
                 !! invalidates that maths. The original version damped every
                 !! axis at once (limit 0.6 / dampen 0.42), which flattened the
                 !! arc into a cloud that stalled in place and sank.
                 The X/Z drag is what spends the outward impulse fast, so the
                 cloud opens fully by the apex and then coasts.
  Rotation       separate axes, +/-480 deg/s on X and Y, +/-700 on Z. Faster on
                 the camera axis because that spin is what reads in an ortho 2D view.
  Size over life flat 1.0 hold to 0.45, then down to 0 at 1.0 (mid 0.80 -> 0.66).
                 Full size through the clump AND the whole rise, so the shards
                 stay chunky inside the box; only the FALL tapers them.
  Colour o/life  colour held CONSTANT; alpha holds to 0.65 then fades. The fade
                 belongs to the fall - fading at 0.40 dissolved the cloud at the
                 top of the arc.
  Renderer       Mesh mode, 4 meshes, sortMode None, shadows/probes off.

  Deliberately OFF: Noise (reads as smoke), Collision (most expensive Shuriken
  module on mobile), Trails and Sub Emitters (double the draw calls).

HOW IT CONNECTS
---------------
  - MatchResolver.FireBurst() calls Play(). Both clear paths reach it:
    ClearGroup -> ScaleDownAndExplode and MergePieceInto ->
    FadeAndScaleDownThenDestroy, which both delegate to CollapseThenBurst.
  - Falls back to FractureObject.ExplodeAtPosition if no ShardBurst is found,
    so removing this script degrades rather than breaks.
  - Reads BoardGridXY.CellSize to size the emitter box (defaults to 1.086).

NOTES
-----
  - The shard meshes in Assets/Resources/VFX/ were baked out of
    Assets/Arts/VFX/NewCubeFrags.fbx (Blender Cell Fracture output, 60 pieces).
    Four were picked for distinct silhouettes - chunky, wedge, splinter, rubble -
    then re-centred on their bounding box, scaled so the longest axis is exactly
    1 unit, and flat-shaded by un-sharing every vertex. 12-20 triangles each.
    The re-centring matters: an off-centre pivot makes a shard orbit instead of
    tumble. If you rebake, keep both steps.
  - Because the meshes are normalised to 1 unit, shardSizeRange reads directly
    in world units.
  - enableGPUInstancing is OFF: Blasty/ShardUnlit has no instancing path yet.
    Adding one (procedural:ParticleInstancingSetup) would collapse a burst to a
    single draw call - worthwhile if bursts ever overlap heavily.
  - The gameplay scenes contain no Light or Light2D, and the URP 2D Renderer
    never draws a UniversalForward pass, so a Lit material would be invisible.
  - COLOUR comes from PieceTintSampler as THREE bands - the block's own painted
    bevel / body / highlight. MatchResolver samples the piece before destroying
    it and passes the TintBands into Play(). The serialized `palette` array is
    only the fallback for when sampling fails.
    !! Do NOT go back to a hand-maintained colour table. The old one was
    !! mis-mapped - PieceColorPalette.cs (dead, unreferenced) uses a completely
    !! different id order than FractureObject - which is what made the shard
    !! colours look wrong in the first play-test. Worse, PieceSimple.colorId
    !! itself COLLIDES: id 8 is used by Orange, Red AND Yellow sprites. Any
    !! palette indexed by colorId is unfixable by construction.
  - !! ONE MATERIAL PER POOLED SYSTEM, not one shared material. The three band
    colours are material properties, because the particle COLOR stream carries
    only one colour per particle and we need three. Two bursts of different
    colours can be alive at once, so they must not share a material.
    MaterialPropertyBlock is NOT an option here: the properties live in
    UnityPerMaterial and the SRP Batcher ignores per-renderer overrides of those.
    main.startColor is set to WHITE - the colourOverLifetime module then
    contributes its alpha ramp and nothing else.
  - !! The shader does NO shading multiply, on purpose. It picks one of the three
    flat colours per facet by angle. The previous version used
    shade = lerp(_Ambient, 1.0, ndl), which cannot exceed 1.0, so every shard was
    darker than the block: measured mean shade 0.646 and 31% of the visible facet
    area pinned at the 0.42 floor. Yellow #F2C14E reached the screen as #C89E3E
    mud. If the shards ever look dull again, look here first.
  - _LightDir points mostly AT THE VIEWER (-0.30, 0.60, -0.74), which looks wrong
    for a key light but is correct here: with backface culling the visible facets
    are the camera-facing ones, so a steep light leaves most of them at ndl = 0.
    The old (-0.35, 0.85, -0.40) put 31% of visible area there; this puts 13%.
  - Verified 2026-08-25 in the Editor: 80 particles for a 2x1 footprint, mesh
    mode, 4 meshes, Default/20; a 4-frame filmstrip at t=0.10/0.40/0.65/0.90
    showing the hold-then-shrink tail with the tint held constant; and all 10
    stack families sampling to the same hex values as an independent read of the
    source PNGs.
  - Verified 2026-08-26 (arc) by simulating a 2-cell burst in edit mode and
    reading ps.GetParticles() at 1-frame intervals across the whole life:
        highest  Y = +1.22 cells  (target +1.20)
        deepest  Y = -2.66 cells  (target -2.25)
        widest  |X| =  1.45 cells (target <= 1.50)
        clumps at t=0.015: 40 left / 40 right, max radius 0.27 cells
        all shards dead by t=0.86
    The small depth overshoot is exactly clusterRadiusCells - a shard spawning
    on the low edge of its clump ends that much lower. Expected, not drift.
  - Verified 2026-08-26 (spread front-loading). Width from the cells, per frame:
        t=0.02 -> 0.11    t=0.08 -> 0.28    t=0.16 -> 0.38
        t=0.315 (apex) -> 0.42, then flat 0.44-0.47 for the rest of the fall.
    i.e. the cloud is at full width by the apex and coasts, instead of still
    opening as it dies.
  - Re-verified 2026-08-26 AFTER the retune (34 shards, +20% size, T=0.58):
        highest +1.17   deepest -2.78   widest |X| 1.41
        68 shards for a 2-cell piece (was 80), last one dead at t=0.57
    The horizontal calibration survived the shorter duration unchanged, because
    the drag spends the impulse in the first ~0.15 s either way.
    The deeper floor reading is a MEASUREMENT artifact, not drift: gravity is now
    56 u/s^2, so a shard covers ~0.2 cells in a single simulate step and the
    sweep catches it just past its death point. Size is ~0 and alpha ~0 by then.
  - Verified 2026-08-26 (multi-piece). Three pieces fired back to back land on
    three separate pooled systems at their OWN cells:
        ShardBurst_0  n=40  X[-3.22,-2.77]
        ShardBurst_1  n=80  X[-0.21, 1.22]     (a 2-cell piece)
        ShardBurst_2  n=40  X[ 2.72, 3.26]  Y[1.96,2.29]
  - Verified 2026-08-26 (colour) by rendering a block and a shard cloud through
    the same camera and histogramming both:
        Yellow  block mean #DEAE4A  ->  shards #E3B452
        Blue    block mean #3178BC  ->  shards #3980C4
        Green   block mean #29BA8E  ->  shards #2CC595
        Red     block mean #C44C3F  ->  shards #C65145
    with the body band covering 62-64% of the pixels in BOTH, matching the
    block's own flat-shaded split. Before the change yellow shards came out
    #C89E3E against a #DEAE4A block.
